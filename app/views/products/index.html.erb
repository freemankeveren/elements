<h1>Products</h1>



<div class="row">
  <% @products.each do |product| %>
    <div class="col s4">
      <p><%= product.name %> - $<%= product.price %></p>
      <%= link_to 'Add to Cart', add_to_cart_path(product), method: :post, class: 'btn' %>

      </div>

  <% end %>
</div>
